FROM python:3.14-slim
WORKDIR /app
RUN pip install --no-cache-dir fastapi uvicorn[standard] pydantic
COPY mock_brivo/ mock_brivo/
CMD ["uvicorn", "mock_brivo.main:app", "--host", "0.0.0.0", "--port", "8001"]
